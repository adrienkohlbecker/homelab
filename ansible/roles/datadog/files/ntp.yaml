init_config:

instances:
  - offset_threshold: 60

    # Optional params:
    #
    # hosts:
    #  - 0.europe.pool.ntp.org
    #  - 1.europe.pool.ntp.org
    #  - 2.europe.pool.ntp.org
    #  - 3.europe.pool.ntp.org
    # port: ntp
    # version: 3
    # timeout: 5
