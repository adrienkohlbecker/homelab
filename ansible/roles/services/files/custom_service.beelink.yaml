---

instances:
  - health_status: none
    container_names:
      - homeassistant
    min_collection_interval: 30

  - health_status: healthy
    container_names:
      - pihole
    min_collection_interval: 30
