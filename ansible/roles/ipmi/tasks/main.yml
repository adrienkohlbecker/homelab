---

- name: Install freeipmi
  apt:
    pkg: "{{ item }}"
    state: present
  with_items:
    - freeipmi-tools
    - freeipmi-bmc-watchdog
    - freeipmi-ipmiseld
  become: yes
  tags:
    - ipmi

# TODO :
# - configure bmc watchdog
# - configure sel logging
# - alerting
