# Generated by zbm-builder.sh at build time
dracut.conf.d/user_hooks.conf
mkinitcpio.conf.d/

# Recovery SSH host private key — staged from 1Password by build.sh at build
# time, shredded on exit. Only the .pub is committed (for known_hosts pinning).
dropbear/ssh_host_ed25519_key
