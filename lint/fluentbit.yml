---
# Fluent Bit calls these Lua filters by name through its `call` config key.
base: lua51
name: fluentbit
globals:
  set_priority:
    args:
      - type: any
      - type: any
      - type: any
  shape_lnav:
    args:
      - type: any
      - type: any
      - type: any
  strip_ansi:
    args:
      - type: any
      - type: any
      - type: any
