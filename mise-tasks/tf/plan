#!/usr/bin/env bash
#MISE description="tofu plan (forwards args)"
#MISE dir="{{config_root}}/terraform"
set -euo pipefail

exec op run -- env TF_ENCRYPTION="key_provider \"pbkdf2\" \"main\" { passphrase = \"$TF_ENCRYPTION_PASSPHRASE\" }" tofu plan "$@"
