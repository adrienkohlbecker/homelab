# Release-divergent: noble installs the qemu-kvm transitional
# metapackage and lacks the swtpm driver; resolute+ installs qemu-system and
# pulls swtpm. resolute exercises both gates.
ubuntu:
  - resolute
