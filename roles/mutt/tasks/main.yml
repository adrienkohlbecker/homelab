# Read-only viewer and search index for getmail's operator-owned Maildir.
# Design: notes/archive/getmail_webmail_design.md.

- name: Install neomutt, notmuch, and w3m
  apt:
    package:
      - neomutt
      - notmuch
      - w3m
    cache_valid_time: 3600
  become: true
  tags:
    - mutt

- name: Create /opt/mutt config directory
  file:
    path: /opt/mutt
    state: directory
    owner: root
    group: root
    mode: "0755"
  become: true
  tags:
    - mutt

# Keep the header cache and search index on services, outside the read-only
# archive and the operator's dotfiles-managed $HOME.
- name: Create neomutt and notmuch state directories and archive mountpoint
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_id }}"
    mode: "0700"
  loop:
    - /mnt/services/mutt
    - /mnt/services/mutt/headers
    - /mnt/services/mutt/notmuch
    - /mnt/services/mutt/archive
  become: true
  tags:
    - mutt

- name: Install the HTML mailcap
  copy:
    src: mailcap
    dest: /opt/mutt/mailcap
    owner: root
    group: root
    mode: "0644"
    backup: true
  become: true
  tags:
    - mutt

- name: Install the mail-archive launcher
  copy:
    src: mail-archive
    dest: /usr/local/bin/mail-archive
    owner: root
    group: root
    mode: "0755"
    backup: true
    validate: bash -n %s
  become: true
  tags:
    - mutt

# getmail creates the source Maildir before mutt in site.yml; _setup supplies it
# in tests. Report a clear precondition error for standalone --tags mutt runs.
- name: Stat the getmail archive Maildir
  stat:
    path: /mnt/services/getmail/new
  register: _mutt_getmail_maildir
  tags:
    - mutt

- name: Assert the getmail archive Maildir exists
  when: not ansible_check_mode or _mutt_getmail_maildir.stat.exists
  assert:
    that:
      - _mutt_getmail_maildir.stat.exists
      - _mutt_getmail_maildir.stat.isdir
    fail_msg: >-
      /mnt/services/getmail is not a Maildir — the getmail role must converge
      before mutt (it creates and owns the archive this viewer binds read-only).
    quiet: true
  tags:
    - mutt

# Both browsing paths use the read-only bind; getmail writes the source directly.
- name: Manage the read-only archive bind mount
  import_role:
    name: systemd_unit
    tasks_from: unit
  vars:
    systemd_unit_args:
      src: archive.mount.j2
      dest: mnt-services-mutt-archive.mount
  tags:
    - mutt

- name: Install the notmuch mount guard
  copy:
    src: notmuch_pre_new.sh
    dest: /opt/mutt/pre-new
    owner: root
    group: root
    mode: "0755"
    backup: true
    validate: bash -n %s
  become: true
  tags:
    - mutt

- name: Configure notmuch archive index
  template:
    src: notmuch-config.j2
    dest: /opt/mutt/notmuch-config
    owner: root
    group: root
    mode: "0644"
    backup: true
    validate: /usr/bin/notmuch --config %s config list
  become: true
  tags:
    - mutt

- name: Prime the notmuch archive index
  command: /usr/bin/notmuch --config /opt/mutt/notmuch-config new
  args:
    creates: /mnt/services/mutt/notmuch/xapian
  become: true
  become_user: "{{ ansible_user_id }}"
  when: not ansible_check_mode
  tags:
    - mutt

- name: Configure neomutt
  template:
    src: muttrc.j2
    dest: /opt/mutt/muttrc
    owner: root
    group: root
    mode: "0644"
    backup: true
    validate: neomutt -n -F %s -Q folder
  become: true
  tags:
    - mutt

- name: Schedule notmuch archive indexing
  import_role:
    name: systemd_timer
    tasks_from: install
  vars:
    systemd_timer_args:
      name: mutt_notmuch
      description: Index the getmail archive with notmuch
      # Runs after the 00:10 getmail timer's normal fetch window.
      on_calendar: "*-*-* 00:30:00"
      exec_start: /usr/bin/notmuch --config /opt/mutt/notmuch-config new
      run_as: "{{ ansible_user_id }}"
      timeout_start_sec: 30m
      additional_service_directives:
        - "[Unit]"
        - RequiresMountsFor=/mnt/services/mutt/archive
  tags:
    - mutt
