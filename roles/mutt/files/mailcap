# Render HTML mail inline as text (archived Gmail is heavily HTML). w3m is
# installed alongside neomutt by the mutt role.
text/html; w3m -dump -T text/html -o display_link_number=1 %s; copiousoutput; nametemplate=%s.html
