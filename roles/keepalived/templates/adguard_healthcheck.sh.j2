#!/bin/bash
set -euo pipefail

answer="$(dig +short +timeout=1 +tries=2 @{{ ansible_facts.default_ipv4.address }} adguard.home A | awk 'NF { print; exit }')"

if [ "$answer" != "127.0.0.1" ]; then
  echo >&2 "Error: adguard.home returned ${answer:-<empty>}, expected 127.0.0.1"
  exit 1
fi
