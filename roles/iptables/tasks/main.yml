- name: Install iptables-persistent
  apt:
    pkg:
      - iptables-persistent
    cache_valid_time: 3600
  become: true
  tags:
    - iptables

# rules.v6 drops everything in the filter table, so IPv6 traffic
# never reaches userspace anyway. Disable at the kernel layer too so
# services that bind on `::` (e.g. transmission) don't sit on a
# silent dead listener and so neighbor discovery doesn't churn.
- name: Disable IPv6 at the kernel level
  ansible.posix.sysctl:
    name: "{{ item }}"
    value: '1'
    sysctl_set: true
    state: present
    reload: true
  loop:
    - net.ipv6.conf.all.disable_ipv6
    - net.ipv6.conf.default.disable_ipv6
  become: true
  tags:
    - iptables

- name: Configure iptables (v4)
  copy:
    src: rules.v4
    dest: /etc/iptables/rules.v4
    owner: root
    group: root
    mode: "0644"
    backup: true
  register: iptables_v4
  become: true
  tags:
    - iptables

- name: Apply iptables v4
  when: iptables_v4.changed
  command: iptables-restore -n /etc/iptables/rules.v4
  changed_when: true
  become: true
  tags:
    - iptables

- name: Configure iptables (v6)
  copy:
    src: rules.v6
    dest: /etc/iptables/rules.v6
    owner: root
    group: root
    mode: "0644"
    backup: true
  register: iptables_v6
  become: true
  tags:
    - iptables

- name: Apply iptables v6
  when: iptables_v6.changed
  command: ip6tables-restore -n /etc/iptables/rules.v6
  changed_when: true
  become: true
  tags:
    - iptables
