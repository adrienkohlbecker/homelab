# secret.yml's _setup installs podman (and the rootless _verify scenario
# layers subid + linger on top); box_deps pre-bakes podman, so route here
# to skip ~140s of podman install on every test run. --machine on the CLI
# still wins. Same rationale as roles/macvlan/meta/test.yml.
machine: box_deps
