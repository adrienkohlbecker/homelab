# podman_secret is a helper role accessed only via `tasks_from: secret`.
# main.yml exists so testrole.py can target this role; the actual logic
# lives in secret.yml and the verification scenarios in _verify.yml.
