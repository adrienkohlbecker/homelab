---
# Per-peer client config + QR + zip generation. Runs only via the
# `wireguard.yml` entry point (mise run ansible:wireguard) on the
# operator's laptop -- NOT imported from main.yml, so `mise run
# ansible --tags wireguard` against a server doesn't touch the
# operator's working tree.
#
# One split-tunnel config per client peer (`home.conf`): wg0 is a
# break-glass backbone reaching peer hosts by /32. Full-tunnel/exit and
# subnet access are the tailscale mesh's job, so the old client x server
# all-traffic cartesian product is gone -- the render/QR tasks loop
# straight over the client peers.

# Peer names flow unquoted into the QR shell command below (and into
# shell-redirected file paths). The regex bounds names to
# [a-z][a-z0-9_]* so a `$`, backtick, space, or `;` in a future
# inventory entry can't turn the localhost play into RCE against the
# operator. Underscores match the repo-wide identifier convention.
- name: Assert wireguard peer names are shell-safe
  assert:
    that: item.name is match('^[a-z][a-z0-9_]*$')
    fail_msg: "wireguard peer name {{ item.name | to_json }} must match ^[a-z][a-z0-9_]*$"
  loop: "{{ wireguard_peers }}"
  loop_control:
    label: "{{ item.name }}"
  tags:
    - wireguard

- name: Create wireguard artifact root
  file:
    dest: "{{ playbook_dir }}/wireguard"
    state: directory
    owner: "{{ ansible_facts.user_uid }}"
    group: "{{ ansible_facts.user_gid }}"
    mode: "0700"
  tags:
    - wireguard

- name: Create wireguard per-peer artifact dir
  file:
    dest: "{{ playbook_dir }}/wireguard/{{ item }}"
    state: directory
    owner: "{{ ansible_facts.user_uid }}"
    group: "{{ ansible_facts.user_gid }}"
    mode: "0700"
  loop: "{{ wireguard_peers | rejectattr('is_server') | map(attribute='name') }}"
  tags:
    - wireguard

- name: Render wireguard client config
  template:
    src: "{{ playbook_dir }}/roles/wireguard/templates/wg.conf.j2"
    dest: "{{ playbook_dir }}/wireguard/{{ item }}/home.conf"
    owner: "{{ ansible_facts.user_uid }}"
    group: "{{ ansible_facts.user_gid }}"
    mode: "0600"
  vars:
    own_peer_name: "{{ item }}"
  loop: "{{ wireguard_peers | rejectattr('is_server') | map(attribute='name') | list }}"
  tags:
    - wireguard

# umask 077 makes qrencode's output land at 0600 (the PNG embeds the
# same secret as the .conf).
- name: Render wireguard QR code
  shell: set -euo pipefail; umask 077; if [ -f "{{ playbook_dir }}/wireguard/{{ item }}/qr.png" ]; then bfr=`md5sum "{{ playbook_dir }}/wireguard/{{ item }}/qr.png" | cut -f 1 -d " "`; else bfr="none"; fi; qrencode -t png -o "{{ playbook_dir }}/wireguard/{{ item }}/qr.png" -r "{{ playbook_dir }}/wireguard/{{ item }}/home.conf"; aftr=`md5sum "{{ playbook_dir }}/wireguard/{{ item }}/qr.png" | cut -f 1 -d " "`; test $bfr = $aftr || ([ "$?" = 1 ] && echo "file updated" || exit 1)
  args:
    executable: /bin/bash
  register: wireguard_qr_result
  changed_when: "'file updated' in wireguard_qr_result.stdout"
  loop: "{{ wireguard_peers | rejectattr('is_server') | map(attribute='name') | list }}"
  tags:
    - wireguard

- name: Bundle wireguard client artifacts into zip
  archive:
    path:
      - "{{ playbook_dir }}/wireguard/{{ item }}/*.conf"
    dest: "{{ playbook_dir }}/wireguard/{{ item }}/{{ item }}.zip"
    format: zip
    owner: "{{ ansible_facts.user_uid }}"
    group: "{{ ansible_facts.user_gid }}"
    mode: "0600"
  loop: "{{ wireguard_peers | rejectattr('is_server') | map(attribute='name') }}"
  tags:
    - wireguard

# Without this prune, renaming `phone` -> `mobile` in wireguard_peers
# leaves `./wireguard/phone/home.conf` (a private-key-bearing file) on
# disk forever, and the next zip-export accidentally includes it. No
# `psk` exclude anymore: PSKs are derived (filter_plugins/wireguard_psk.py),
# so a leftover `psk/` dir from the old file-based scheme is itself an
# orphan and gets swept here.
- name: Find existing wireguard peer dirs
  find:
    paths: "{{ playbook_dir }}/wireguard"
    file_type: directory
    depth: 1
  register: wireguard_peer_dirs_found
  tags:
    - wireguard

- name: Prune orphan wireguard peer dirs
  file:
    path: "{{ item.path }}"
    state: absent
  loop: "{{ wireguard_peer_dirs_found.files | default([]) }}"
  loop_control:
    label: "{{ item.path | basename }}"
  when: (item.path | basename) not in (wireguard_peers | rejectattr('is_server') | map(attribute='name') | list)
  tags:
    - wireguard

# Sweep a leftover `psk/` dir from the old file-based scheme (now an
# orphan -- not a peer name) so private-key-adjacent PSK files don't
# linger. Handled by the peer-dir prune above (the `psk` exclude is gone).
