# _setup.yml imports test/nginx, which is pre-baked into box_deps (see
# packer/seed_deps.yml), so route there to skip the nginx install on every
# test run. minio needs nothing else box_deps lacks.
machines:
  box_deps:
