# Authelia emits one JSON object per stdout line. Keep journald's arrival time
# as the aggregate event time; the application timestamp is redundant and the
# post-parser normalizer removes it.
[PARSER]
    Name    authelia_json
    Format  json
