- name: Set hostname
  hostname:
    name: "{{ inventory_hostname }}"
  become: true
  tags:
    - hostname

- name: Setup hosts file
  template:
    src: hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: "0644"
    backup: true
  become: true
  tags:
    - hostname
