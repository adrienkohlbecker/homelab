# minimal is the vanilla cloud image (cloud-init present); box is the
# debootstrap'd ZFS box (no cloud-init). Covering both proves the hostname
# and /etc/hosts tasks converge cleanly whether or not cloud-init coexists.
machines:
  box:
  minimal:
