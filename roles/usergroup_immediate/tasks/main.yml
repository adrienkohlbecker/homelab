- name: Add user to group
  user:
    name: "{{ ansible_ssh_user }}"
    groups: "{{ group }}"
    append: true
  register: usergroup
  become: true

- when: usergroup.changed
  include_tasks: reset_connection.yml
