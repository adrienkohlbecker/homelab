- name: Configure certificate metrics
  import_role:
    name: netdata
    tasks_from: config
  vars:
    netdata_args:
      src: x509check.conf.j2
      dest: go.d/x509check.conf
      condition: "{{ certbot_netdata_condition | default(true) }}"
  tags:
    - certbot
