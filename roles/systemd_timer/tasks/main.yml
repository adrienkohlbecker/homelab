# systemd_timer is a helper role accessed only via `tasks_from: {install,remove}`.
# main.yml exists so testrole.py can target this role; the actual logic lives
# in the named subtask files and the verification scenarios in _verify.yml.
