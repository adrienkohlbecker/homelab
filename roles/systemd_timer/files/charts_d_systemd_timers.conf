# Managed by ansible (roles/systemd_timer/files/charts_d_systemd_timers.conf).
# Sourced by /usr/libexec/netdata/charts.d/systemd_timers.chart.sh before
# systemd_timers_check. Per-timer metadata (period_secs) lives in the .d
# directory below; ansible's roles/systemd_timer install task writes one
# file per registered timer there.
systemd_timers_meta_dir=/etc/netdata/charts.d/systemd_timers.d
