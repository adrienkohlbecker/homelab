# Operationally empty. The harness applies mirrors.yml as its pre-role
# bootstrap, and testrole.py test invokes _verify.yml against that live state.
# Service-specific fixtures live with their owning roles.
