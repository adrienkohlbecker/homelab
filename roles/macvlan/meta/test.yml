# The podman_network path needs podman, which box_deps pre-bakes (see
# packer/seed_deps.yml) — route there to skip ~140s of podman install on
# every test run. --machine on the CLI still wins over this.
machine: box_deps
