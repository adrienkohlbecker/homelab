# compression.yml toggles the bare `compress` directive per zfs_root:
# ZFS hosts disable it (+ dpkg_divert); ext4 hosts enable it (+ remove
# divert). The minimal cell exercises the non-ZFS enable path.
machines:
  box:
  minimal:
