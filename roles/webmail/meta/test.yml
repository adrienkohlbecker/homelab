# This role runs Dovecot + Roundcube containers behind nginx; _setup.yml imports
# podman/install + test/nginx + the authelia stub, all pre-baked into box_deps
# (see packer/seed_deps.yml), so route there to skip ~140s of backports + podman
# + nginx install on every test run.
machines:
  box_deps:
