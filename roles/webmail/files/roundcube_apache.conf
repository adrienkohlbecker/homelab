# Mounted into the roundcube container at /etc/apache2/conf-enabled/. nginx
# fronts roundcube, so apache's own ServerName is cosmetic — setting it just
# silences the "AH00558: Could not reliably determine the server's fully
# qualified domain name" line apache logs at every startup.
ServerName localhost
