# deb_install is a helper role accessed only via `tasks_from: install`. main.yml
# exists so testrole.py can target this role; the install logic lives in
# install.yml and the verification scenarios in _verify.yml.
