- name: Install gpg
  apt:
    pkg: gpg
    cache_valid_time: 3600
  become: true
  register: apt_install_gpg
  tags:
    - user

- name: Install packages
  apt:
    pkg:
      - attr
      - ccze
      - curl
      - fdupes
      - git
      - git-lfs
      - gparted
      - htop
      - iotop
      - iperf3
      - jq
      - lsof
      - make
      - man-db
      - moreutils
      - mosh
      - ncdu
      - nmon
      - nnn
      - nvme-cli
      - pandoc
      - pass
      - progress
      - rsync
      - screen
      - socat
      - strace
      - stress
      - sysstat
      - telnet
      - tig
      - tmux
      - tree
      - usbutils
      - vim
      - zsh
    cache_valid_time: 3600
  become: true
  tags:
    - user

- name: Set shell to zsh
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh
  become: true
  tags:
    - user

- name: Sudo without password
  copy:
    dest: /etc/sudoers.d/{{ ansible_user }}
    content: "{{ ansible_user }} ALL=(ALL) NOPASSWD:ALL\n"
    validate: visudo -cf %s
    backup: true
    owner: root
    group: root
    mode: "0440"
  become: true
  tags:
    - user

- name: Cleanup sudoers added by cloud-init
  file:
    path: /etc/sudoers.d/90-cloud-init-users
    state: absent
  become: true
  tags:
    - user

- name: Add authorized keys
  authorized_key:
    user: "{{ ansible_user }}"
    key: "{{( ssh_public_keys + '\n' + (ssh_public_keys_additional | default(''))) | trim }}"
    exclusive: true
  tags:
    - user

- name: Configure git
  template:
    src: gitconfig.j2
    dest: "{{ ansible_user_dir }}/.gitconfig.local"
    mode: "0644"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    backup: true
  tags:
    - user

- name: Get public key
  when: not (ansible_check_mode and apt_install_gpg.changed)
  command: gpg --keyserver keyserver.ubuntu.com --recv-keys C248DE6357445D6302F9A62E74BFD03C20CC21AF
  args:
    creates: "{{ ansible_user_dir }}/.gnupg/pubring.kbx"
  tags:
    - user

- name: Install dotfiles repo
  command: git clone --bare https://github.com/adrienkohlbecker/dotfiles.git .dotfiles
  args:
    chdir: "{{ ansible_user_dir }}"
    creates: "{{ ansible_user_dir }}/.dotfiles"
  tags:
    - user

- name: Install dotfiles
  command: git --git-dir={{ ansible_user_dir }}/.dotfiles --work-tree={{ ansible_user_dir }} reset --hard
  args:
    chdir: "{{ ansible_user_dir }}"
    creates: "{{ ansible_user_dir }}/.zshrc"
  tags:
    - user

- name: Fix git config
  when: ansible_distribution_version == "22.04"
  command: sed -i 's/zdiff3/diff3/' .gitconfig
  args:
    chdir: "{{ ansible_user_dir }}"
  tags:
    - user

- name: Install dotfiles submodules
  command: git --git-dir={{ ansible_user_dir }}/.dotfiles --work-tree={{ ansible_user_dir }} submodule update --init --recursive
  args:
    chdir: "{{ ansible_user_dir }}"
    creates: "{{ ansible_user_dir }}/.zsh/pure/readme.md"
  tags:
    - user

- name: Check installed asdf version
  command: "{{ ansible_user_dir }}/.asdf/bin/asdf --version"
  register: asdf_check
  failed_when: false
  changed_when: false
  check_mode: false
  tags:
    - user

- name: Create asdf bin directory
  when: asdf_version not in asdf_check.stdout
  file:
    path: "{{ ansible_user_dir }}/.asdf/bin"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  tags:
    - user

- name: Download asdf archive
  when: asdf_version not in asdf_check.stdout
  check_mode: false
  get_url:
    url: "{{ asdf_release[ansible_architecture].url }}"
    checksum: "md5:{{ asdf_release[ansible_architecture].md5 }}"
    dest: /tmp/asdf-{{ asdf_version }}.tar.gz
    mode: "0644"
  tags:
    - user

- name: Install asdf binary
  when: asdf_version not in asdf_check.stdout
  unarchive:
    src: /tmp/asdf-{{ asdf_version }}.tar.gz
    remote_src: true
    dest: "{{ ansible_user_dir }}/.asdf/bin"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  tags:
    - user

- name: Create asdf completions directory
  when: asdf_version not in asdf_check.stdout
  file:
    path: "{{ ansible_user_dir }}/.asdf/completions"
    state: directory
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0755"
  tags:
    - user

- name: Generate asdf zsh completions
  when: asdf_version not in asdf_check.stdout
  shell: "{{ ansible_user_dir }}/.asdf/bin/asdf completion zsh > {{ ansible_user_dir }}/.asdf/completions/_asdf"
  tags:
    - user

- name: Delete asdf archive
  when: asdf_version not in asdf_check.stdout
  file:
    path: /tmp/asdf-{{ asdf_version }}.tar.gz
    state: absent
  tags:
    - user

- name: Add 1password repository key
  when: not (ansible_check_mode and apt_install_gpg.changed)
  apt_key:
    id: AC2D62742012EA22
    url: https://downloads.1password.com/linux/keys/1password.asc
    keyring: /etc/apt/trusted.gpg.d/1password.gpg
  become: true
  tags:
    - user

- name: Add 1password repository
  apt_repository:
    repo: deb [arch={{ (ansible_architecture == "aarch64") | ternary('arm64', 'amd64') }} signed-by=/etc/apt/trusted.gpg.d/1password.gpg] https://downloads.1password.com/linux/debian/{{ (ansible_architecture == "aarch64") | ternary('arm64', 'amd64') }} stable main
  become: true
  register: apt_repository_1password
  tags:
    - user

- name: Install packages
  when: not (ansible_check_mode and apt_repository_1password.changed)
  apt:
    pkg:
      - 1password-cli
    cache_valid_time: 3600
  become: true
  tags:
    - user
