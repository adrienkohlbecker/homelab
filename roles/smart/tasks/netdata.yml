- name: Configure smart metrics
  import_role:
    name: netdata
    tasks_from: copy
  vars:
    netdata_args:
      src: smartctl.conf
      dest: go.d/smartctl.conf
      restart: "{{ netdata_restart | default(true) }}"
  tags:
    - smart
