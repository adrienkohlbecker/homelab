- name: Configure lm_sensors metrics
  import_role:
    name: netdata
    tasks_from: copy
  vars:
    netdata_args:
      src: sensors.conf
      dest: go.d/sensors.conf
      restart: "{{ netdata_restart }}"
  tags:
    - lm_sensors
