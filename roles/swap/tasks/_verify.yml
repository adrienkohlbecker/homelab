# Swap role _verify: assert zram is active and primary, an optional partition
# coexists below it, and swappiness is tuned. testrole.py / testall.py only
# (qemu); never site.yml. See notes/swap_strategy.md.

- name: _verify | Read active swap devices with priorities and sizes
  command: swapon --show=NAME,PRIO,SIZE --noheadings --bytes --raw
  changed_when: false
  become: true
  register: _swap_verify_swapon

- name: _verify | Zram swap active at the primary priority
  vars:
    _zram_lines: "{{ _swap_verify_swapon.stdout_lines | select('search', 'zram') | list }}"
  assert:
    that:
      - _zram_lines | length == 1
      - _zram_lines[0].split()[1] | int == 100
    fail_msg: >-
      zram swap missing or not at priority 100 (partition swap sits at
      priority 10): {{ _swap_verify_swapon.stdout }}

# Catches the generator silently falling back to its default size (e.g. a
# size key the installed version doesn't honor). Expected = min(configured,
# MemTotal); zram rounds down by a page, so allow a small band.
# SIZE (field 3, bytes) comes directly from swapon --show --bytes; no slurp needed.
- name: _verify | Zram device sized per config (capped at RAM)
  vars:
    _zram_line: "{{ _swap_verify_swapon.stdout_lines | select('search', 'zram') | first | default('') }}"
    _expected_mb: "{{ [(swap_zram_size | human_to_bytes) // 1048576, ansible_memtotal_mb] | min }}"
    _actual_mb: "{{ ((_zram_line.split()[2] | default('0')) | int) // 1048576 }}"
  assert:
    that:
      - _actual_mb | int >= (_expected_mb | float * 0.95) | int
      - _actual_mb | int <= (_expected_mb | float * 1.05) | int
    fail_msg: >-
      zram0 is {{ _actual_mb }} MB, expected ~{{ _expected_mb }} MB (min of
      configured {{ swap_zram_size }} and MemTotal {{ ansible_memtotal_mb }} MB)
      -- check zram-fraction/max-zram-size in zram-generator.conf.

- name: _verify | Resolve partition device path
  when: swap_partition is defined
  command: readlink -f {{ swap_partition }}
  changed_when: false
  become: true
  register: _swap_verify_partition_dev

- name: _verify | Partition swap device is active
  when: swap_partition is defined
  vars:
    _names: "{{ _swap_verify_swapon.stdout_lines | map('split') | map('first') | list }}"
  assert:
    that:
      - (_swap_verify_partition_dev.stdout | trim) in _names
    fail_msg: >-
      partition swap device {{ _swap_verify_partition_dev.stdout | trim }}
      (for {{ swap_partition }}) not active: {{ _swap_verify_swapon.stdout }}

# On the mirror hosts the swap partition is an mdadm raid1 whose redundancy is
# the reason it exists -- the active-swap assert above proves it's usable but
# not that the mirror is intact. A regression that assembled the wrong level
# or left the array degraded would pass swapon yet silently lose fault
# tolerance. Gated on the resolved device being an md node, so the plain
# partition fixtures (box) skip it. (Initial-create resync may still be
# running here -- harmless: all members are active-sync throughout, so the
# level/failed-count asserts hold; we deliberately don't gate on 'clean' to
# avoid racing the resync.)
- name: _verify | Inspect the swap raid array when swap is on mdadm
  when:
    - swap_partition is defined
    - (_swap_verify_partition_dev.stdout | trim).startswith('/dev/md')
  block:
    - name: _verify | Read mdadm detail for the swap array
      command: mdadm --detail {{ swap_partition }}
      become: true
      changed_when: false
      register: _swap_verify_md_detail

    - name: _verify | Assert the swap array is a healthy raid1
      assert:
        that:
          - "'Raid Level : raid1' in _swap_verify_md_detail.stdout"
          - "'Failed Devices : 0' in _swap_verify_md_detail.stdout"
          - "'degraded' not in _swap_verify_md_detail.stdout"
        fail_msg: >-
          The swap array is not a healthy raid1. mdadm --detail:
          {{ _swap_verify_md_detail.stdout }}

- name: _verify | Overflow swap priority is below zram
  when: swap_partition is defined
  vars:
    _overflow_lines: "{{ _swap_verify_swapon.stdout_lines | reject('search', 'zram') | list }}"
  assert:
    that:
      - _overflow_lines | length >= 1
      - _overflow_lines | map('split') | map(attribute='1') | map('int') | max < 100
    fail_msg: >-
      Overflow swap priority must be below zram (100), got:
      {{ _swap_verify_swapon.stdout }}

- name: _verify | Read vm.swappiness
  command: sysctl -n vm.swappiness
  changed_when: false
  register: _swap_verify_swappiness

- name: _verify | Swappiness tuned to the configured value
  assert:
    that:
      - (_swap_verify_swappiness.stdout | trim | int) == (swap_swappiness | int)
    fail_msg: >-
      vm.swappiness is {{ _swap_verify_swappiness.stdout | trim }},
      expected {{ swap_swappiness }}.
