# box exercises zram + the raw-partition swapon/fstab path (it declares
# swap_partition over its baked p3 — folding in what was pug's unique
# coverage); minimal exercises zram-only on ext4 (no partition); lab
# exercises mdadm raid1 swap across its three p3 members.
machines:
  box:
  minimal:
  pug:
  lab:
