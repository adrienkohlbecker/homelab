# box exercises zram + zvol (ZFS-backed); minimal exercises zram-only
# on ext4 (no zvol, no partition); pug exercises zram + raw-partition swap
# (the only host with swap_partition — fstab + swapon path in main.yml).
machines:
  box:
  minimal:
  pug:
