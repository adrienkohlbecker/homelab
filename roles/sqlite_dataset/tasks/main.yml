# sqlite_dataset is a helper role accessed only via named task files.
# main.yml exists so testrole.py can target this role; the actual logic
# lives in dataset.yml and the verification scenarios in _verify.yml.
