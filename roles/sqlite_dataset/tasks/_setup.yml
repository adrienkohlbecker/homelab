- name: _setup | Create fixture directories
  file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  become: true
  loop:
    - /mnt/services/sqlite
    - /mnt/services/sqlite_dataset_test
    - /mnt/services/sqlite_dataset_test/sub
