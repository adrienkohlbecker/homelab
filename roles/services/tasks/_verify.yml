# Functional verify for the datasets created by main.yml.
#
# An idempotent converge is not evidence extra_properties actually landed --
# community.general.zfs treats a property whose source is not local|received|-
# as absent, so a value silently inherited from the parent reads back correct
# while never being set locally. For the sqlite child this is load-bearing: an
# inherited recordsize would test-pass yet silently defeat Home Assistant's
# 4K-page recorder tuning. `zfs get` source must be `local`,
# not just right -- zfs-autobackup's picker filters with `-s local`
# (roles/zfs_autobackup/templates/zfs_autosnapshot.sh.j2:14), so an inherited
# autobackup:bak=true would test-pass but never get backed up.
#
# Expected values come from services_datasets (vars/main.yml), shared with
# main.yml, so the assertions track whatever both halves agree on.

- name: _verify | Expected properties set locally
  when: zfs_root
  vars:
    services_expected_properties: >-
      {{ {'mountpoint': item.mountpoint} | combine(item.extra_properties) }}
  command:
    argv:
      - zfs
      - get
      - -H
      - -p
      - -s
      - local
      - -o
      - property,value
      - "{{ services_expected_properties.keys() | join(',') }}"
      - "{{ item.dataset }}"
  register: services_verify_properties
  changed_when: false
  failed_when: >-
    (services_verify_properties.stdout_lines | sort)
    != (services_expected_properties.items() | map('join', '\t') | sort)
  loop: "{{ services_datasets }}"
  loop_control:
    label: "{{ item.dataset }}"
  become: true

- name: _verify | Dataset mount contract passes
  when: zfs_root
  command:
    argv:
      - /usr/local/bin/zfs_check_mount
      - "{{ item.dataset }}"
      - "{{ item.mountpoint }}"
  changed_when: false
  loop: "{{ services_datasets }}"
  loop_control:
    label: "{{ item.mountpoint }} <- {{ item.dataset }}"
  become: true

- name: _verify | Mount status units are active
  when: zfs_root
  command:
    argv:
      - systemctl
      - is-active
      - "{{ item.mountpoint | zfs_mount_unit }}"
  changed_when: false
  loop: "{{ services_datasets }}"
  loop_control:
    label: "{{ item.mountpoint | zfs_mount_unit }}"
  become: true
