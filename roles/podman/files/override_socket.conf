# vim:ft=systemd-unit-file
# WARNING: membership in podman-socket grants control of the rootful
# podman API socket — that is host-root-equivalent (a member can
# `podman run --privileged -v /:/host`). Add a user to it only when it
# genuinely needs full podman API access.
[Socket]
SocketGroup=podman-socket
