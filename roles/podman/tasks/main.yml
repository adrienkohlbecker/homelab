- name: Install podman package
  import_role:
    name: podman
    tasks_from: install
  tags:
    - podman

- name: Add podman-socket group
  group:
    name: podman-socket
    system: true
  become: true
  tags:
    - podman

- name: Install podman socket override
  import_role:
    name: systemd_unit
    tasks_from: dropin
  vars:
    systemd_unit_args:
      src: override_socket.conf
      for: podman.socket
  tags:
    - podman

- name: Install podman service override
  import_role:
    name: systemd_unit
    tasks_from: dropin
  vars:
    systemd_unit_args:
      src: override_service.conf
      for: podman.service
  tags:
    - podman

- name: Divert configuration file
  community.general.dpkg_divert:
    path: /usr/lib/tmpfiles.d/podman.conf
  become: true
  tags:
    - podman

- name: Configure tmpfiles.d
  when: not (ansible_check_mode and podman_apt.changed)
  lineinfile:
    regexp: '^(# )?(.*) /run/podman (.*)$'
    line: '# \2 /run/podman \3'
    backrefs: true
    dest: /usr/lib/tmpfiles.d/podman.conf
    backup: true
    owner: root
    group: root
    mode: "0644"
  become: true
  tags:
    - podman

- name: Enable the service
  when: not (ansible_check_mode and podman_apt.changed)
  systemd:
    name: podman
    enabled: true
  become: true
  tags:
    - podman

- name: Enable podman.socket
  import_role:
    name: systemd_unit
    tasks_from: service
  vars:
    systemd_unit_args:
      src: podman.socket
      condition: "{{ not (ansible_check_mode and podman_apt.changed) }}"
    # podman_backports_updated forces a restart when the noble upgrade
    # flipped the binary under us — apt under policy_rc_d=101 leaves
    # the running 3.x daemon on disk, but the new socket on
    # /run/podman would be served by the stale process otherwise.
      restart: "{{ podman_service_d_override_conf_result.changed or podman_socket_d_override_conf_result.changed or (podman_backports_updated | default(false)) }}"
  tags:
    - podman

- name: Configure health script
  file:
    path: /usr/local/bin/wait_for_healthy_container
    state: absent
  become: true
  tags:
    - podman

- name: Install podman prune timer
  import_tasks: prune.yml
  vars:
    podman_prune_args: {}
  tags:
    - podman

# The per-user prune timer below is user-scope; systemd_unit's
# install fail-closes in real-apply when /var/lib/systemd/linger/<user>
# is missing. In site.yml's full run the `user` role enables linger
# for ansible_user_id earlier, so this is a no-op there; under
# testrole.py podman (and under any role that imports podman as a
# dep), the user role hasn't run yet, so enable linger here so
# podman is self-sufficient.
- name: Enable linger for the operator user (required by user-scope prune timer)
  import_role:
    name: linger
    tasks_from: linger
  vars:
    linger_args:
      name: "{{ ansible_user_id }}"
      uid: "{{ ansible_user_uid }}"
  tags:
    - podman

- name: Install user-scope podman prune timer for the operator user
  import_tasks: prune.yml
  vars:
    podman_prune_args:
      user: "{{ ansible_user_id }}"
  tags:
    - podman

# Podman macvlan networks are owned by the macvlan role now — each
# entry in host_vars.macvlan_blocks with `podman_network: true` gets
# its podman network created alongside its host-side iface. See
# roles/macvlan/tasks/install.yml.
