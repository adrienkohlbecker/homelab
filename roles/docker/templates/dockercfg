{
	"auths": {
		"https://index.docker.io/v1/": {
			"auth": "{{ (credentials.dockerhub.user + ":" + credentials.dockerhub.password )| b64encode }}",
			"email": "{{ credentials.dockerhub.email }}"
		}
	}
}
