- name: Create internal podman network
  # macvlan onto lab's IoT VLAN (netplan-named `iot`, vlan id 4 on nic0)
  # so HA has an L2 presence there for mDNS/SSDP discovery of Sonos /
  # HomeKit / Zigbee2MQTT-bridge targets.
  #
  # Carving a /30 out of lab's `iot` /24 deliberately shrinks netavark's
  # allocation pool: .96 is network, .99 is broadcast, .97 is the
  # netavark-assigned gateway, leaving .98 as the single container IP.
  # HA gets .98 (pinned explicitly in the unit template).
  #
  # The earlier choice of .100/30 (.100-.103) overlapped lab's own iot
  # address 10.123.4.102; netavark picked .102 for HA, two MACs claimed
  # the same IP and every HA-originated frame on the IoT VLAN logged as
  # a martian. Lab's address is configured in host_vars/lab.yml under
  # vlans.iot.addresses; any future lab static IP added inside .96-.99
  # would re-trigger the collision -- pick a different /30 if so.
  containers.podman.podman_network:
    name: iot_macvlan
    subnet: 10.123.4.96/30
    driver: macvlan
    opt:
      parent: iot
  become: true
  when: not ansible_check_mode
  tags:
    - homeassistant

# Join the mosquitto-owned network so home-assistant can resolve `mosquitto`
# via aardvark-dns. See paperless/tasks/main.yml for the rationale.
- name: Create mosquitto podman network
  when: not ansible_check_mode
  containers.podman.podman_network:
    name: mosquitto
    disable_dns: false
    state: present
  become: true
  tags:
    - homeassistant

- name: Set up homeassistant system user, group, and config dir
  import_role:
    name: service_user
    tasks_from: user
  vars:
    service_user_args:
      name: homeassistant
  tags:
    - homeassistant

- name: Create sqlite directory
  when: not (ansible_check_mode and homeassistant_user.changed)
  file:
    dest: /mnt/services/sqlite/homeassistant
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Create homeassistant subdirectories
  when: not (ansible_check_mode and homeassistant_user.changed)
  file:
    dest: "/mnt/services/homeassistant/{{ item }}"
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  loop:
    - custom_templates
    - blueprints
    - blueprints/automation
    - www
    - custom_components
  tags:
    - homeassistant

# ZHA stores its zigbee.db under /config alongside YAML/automations. The
# recorder DB already lives on the sqlite dataset via /config/db; pull
# zigbee.db onto the same dataset by symlinking through /config/db so
# SQLite resolves the symlink before computing WAL placement. The symlink
# target is dangling on the host (no /config/db there) but resolves
# inside the container — force: true bypasses ansible's host-side check.
- name: Stat ZHA zigbee.db on host
  stat:
    path: /mnt/services/homeassistant/zigbee.db
    follow: false
  register: homeassistant_zigbee_db_stat
  become: true
  tags:
    - homeassistant

# Guard against `force: true` silently replacing a real ZHA database with
# a dangling symlink. If zigbee.db already exists as a regular file
# (e.g. role applied before this symlink task landed, ZHA created its
# DB at that path), force the operator to manually move it to
# /mnt/services/sqlite/homeassistant/zigbee.db first.
- name: Refuse to clobber a real zigbee.db with a symlink
  assert:
    that: not (homeassistant_zigbee_db_stat.stat.exists and not homeassistant_zigbee_db_stat.stat.islnk)
    fail_msg: >-
      /mnt/services/homeassistant/zigbee.db exists as a regular file; the
      symlink task would replace it and lose ZHA's database. Move it to
      /mnt/services/sqlite/homeassistant/zigbee.db (and remove the
      original) before re-running.
  tags:
    - homeassistant

- name: Symlink ZHA zigbee.db onto sqlite dataset
  file:
    src: /config/db/zigbee.db
    dest: /mnt/services/homeassistant/zigbee.db
    state: link
    owner: homeassistant
    group: homeassistant
    follow: false
    force: true
  become: true
  tags:
    - homeassistant

# The influxdb integration is configured through HA's UI (not via
# configuration.yaml); the bootstrap tasks below land the org / user /
# bucket / write-auth so the operator can paste the auth token from
# `influx auth list --user homeassistant` into HA's UI integration.
# Each task uses `influx ... --json` and parses via `from_json` to
# avoid the column-shape drift that tab-cut parsing carries across
# influxdb-cli versions.
- name: Query influxdb home org
  command: podman exec influxdb influx org list --name home --json
  register: _homeassistant_influxdb_org_query
  changed_when: false
  check_mode: false
  failed_when: >-
    _homeassistant_influxdb_org_query.rc != 0
    or (_homeassistant_influxdb_org_query.stdout | from_json | length) == 0
  become: true
  tags:
    - homeassistant

- name: Set influxdb org_id fact
  set_fact:
    homeassistant_influxdb_org_id: "{{ (_homeassistant_influxdb_org_query.stdout | from_json)[0].id }}"
  tags:
    - homeassistant

- name: Query influxdb homeassistant user
  command: podman exec influxdb influx user list --name homeassistant --json
  register: _homeassistant_influxdb_user_query
  changed_when: false
  check_mode: false
  failed_when: false
  become: true
  tags:
    - homeassistant

- name: Create influxdb homeassistant user  # noqa: no-changed-when -- when: gates execution, like creates: did
  when: >-
    _homeassistant_influxdb_user_query.rc != 0
    or (_homeassistant_influxdb_user_query.stdout | from_json | length) == 0
  command: podman exec influxdb influx user create --org-id {{ homeassistant_influxdb_org_id }} --name homeassistant
  become: true
  tags:
    - homeassistant

- name: Query influxdb homeassistant bucket
  command: podman exec influxdb influx bucket list --org-id {{ homeassistant_influxdb_org_id }} --name homeassistant --json
  register: _homeassistant_influxdb_bucket_query
  changed_when: false
  check_mode: false
  failed_when: false
  become: true
  tags:
    - homeassistant

- name: Create influxdb homeassistant bucket  # noqa: no-changed-when -- when: gates execution, like creates: did
  when: >-
    _homeassistant_influxdb_bucket_query.rc != 0
    or (_homeassistant_influxdb_bucket_query.stdout | from_json | length) == 0
  command: podman exec influxdb influx bucket create --org-id {{ homeassistant_influxdb_org_id }} --name homeassistant
  become: true
  tags:
    - homeassistant

# Re-query post-create so the auth task always sees a concrete bucket_id.
# The conditional create above doesn't emit a usable id (the upstream
# `influx bucket create` output schema changed across 2.x patch releases).
- name: Query influxdb homeassistant bucket id
  command: podman exec influxdb influx bucket list --org-id {{ homeassistant_influxdb_org_id }} --name homeassistant --json
  register: _homeassistant_influxdb_bucket_id_query
  changed_when: false
  check_mode: false
  failed_when: >-
    _homeassistant_influxdb_bucket_id_query.rc != 0
    or (_homeassistant_influxdb_bucket_id_query.stdout | from_json | length) == 0
  become: true
  tags:
    - homeassistant

- name: Set influxdb bucket_id fact
  set_fact:
    homeassistant_influxdb_bucket_id: "{{ (_homeassistant_influxdb_bucket_id_query.stdout | from_json)[0].id }}"
  tags:
    - homeassistant

- name: Query influxdb auth for homeassistant
  command: podman exec influxdb influx auth list --user homeassistant --json
  register: _homeassistant_influxdb_auth_query
  changed_when: false
  check_mode: false
  failed_when: false
  become: true
  tags:
    - homeassistant

# Match on the parsed permissions array (resource.type=buckets,
# resource.id=<homeassistant bucket>, action=write) instead of grepping
# a bracketed scope literal — the previous grep compared the user-id
# column (different in `auth list` vs `auth create` output) against the
# write-scope substring, which silently matched whatever happened to
# line up.
- name: Create influxdb write-auth for homeassistant bucket  # noqa: no-changed-when -- when: gates execution, like creates: did
  when: >-
    (_homeassistant_influxdb_auth_query.stdout | default('[]') | from_json)
    | selectattr('permissions', 'defined')
    | map(attribute='permissions') | flatten
    | selectattr('action', 'equalto', 'write')
    | selectattr('resource.type', 'equalto', 'buckets')
    | selectattr('resource.id', 'equalto', homeassistant_influxdb_bucket_id)
    | list | length == 0
  command: >-
    podman exec influxdb influx auth create
    --org-id {{ homeassistant_influxdb_org_id }}
    --user homeassistant
    --write-bucket {{ homeassistant_influxdb_bucket_id }}
  become: true
  tags:
    - homeassistant

# HA-flavoured YAML uses custom tags (!include, !include_dir_merge_named,
# !secret, ...). yaml.safe_load chokes on those, so register a stub
# constructor that returns None for any !-prefixed tag before parsing.
#
# Scope of this check: well-formedness only -- catches jinja quoting
# bugs, vault-substitution typos that break YAML structure, indentation
# regressions. It does NOT check HA-tag semantics: a typo'd
# `!include automatons.yaml` (target missing) or `!secret no_such_key`
# (key missing) both parse fine here -- the stub returns None and YAML
# is structurally valid. HA's own schema + integration loaders catch
# those at startup, where they're loud but post-deploy.
- name: Configure homeassistant
  template:
    src: configuration.yaml.j2
    dest: /mnt/services/homeassistant/configuration.yaml
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    backup: true
    validate: python3 -c 'import yaml,sys; yaml.add_multi_constructor("!",lambda l,s,n:None,Loader=yaml.SafeLoader); yaml.safe_load(open(sys.argv[1]))' "%s"
  register: homeassistant_conf
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant config files
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: homeassistant
    group: homeassistant
    mode: "{{ item.mode | default('0600') }}"
    backup: true
    validate: "{{ item.validate | default(omit) }}"
  register: homeassistant_config_files
  become: true
  vars:
    _yaml_validate: python3 -c 'import yaml,sys; yaml.add_multi_constructor("!",lambda l,s,n:None,Loader=yaml.SafeLoader); yaml.safe_load(open(sys.argv[1]))' "%s"
  loop:
    - src: macros.jinja
      dest: /mnt/services/homeassistant/custom_templates/macros.jinja
    - src: sensors.yaml
      dest: /mnt/services/homeassistant/sensors.yaml
      validate: "{{ _yaml_validate }}"
    - src: templates.yaml
      dest: /mnt/services/homeassistant/templates.yaml
      validate: "{{ _yaml_validate }}"
    - src: blueprints/automation/hvac_dry_mode.yaml
      dest: /mnt/services/homeassistant/blueprints/automation/hvac_dry_mode.yaml
      mode: "0644"
      validate: "{{ _yaml_validate }}"
  loop_control:
    label: "{{ item.dest }}"
  tags:
    - homeassistant

- name: Ensure files exist
  file:
    path: /mnt/services/homeassistant/{{ item }}
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    state: touch
    modification_time: preserve
    access_time: preserve
  register: homeassistant_files
  become: true
  with_items:
    - automations.yaml
    - scripts.yaml
    - scenes.yaml
  tags:
    - homeassistant

# unarchive's zip codepath shells out to /usr/bin/unzip; ubuntu cloud
# images don't ship it by default.
- name: Install unzip
  apt:
    pkg:
      - unzip
    cache_valid_time: 3600
  become: true
  tags:
    - homeassistant

# Bootstrap HACS. Anything else HACS-installable is intentionally managed
# via the HACS UI inside HA -- adding it twice double-loads its frontend
# resources. Examples currently managed via HACS: thermal_comfort,
# thermal_comfort_icons. The version, URL, and sha256 stay together so a
# bump touches one place; the `creates:` sentinel + checksum-pinned
# download give content-pinning without an on-disk version file.
- name: Create HACS download cache dir
  file:
    path: /var/cache/homeassistant
    state: directory
    owner: root
    group: root
    mode: "0700"
  become: true
  tags:
    - homeassistant

- name: Create HACS install dir
  when: not (ansible_check_mode and homeassistant_user.changed)
  file:
    path: /mnt/services/homeassistant/custom_components/hacs
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Download HACS archive
  get_url:
    url: "{{ mirror_github }}/hacs/integration/releases/download/2.0.5/hacs.zip"
    checksum: sha256:97be6b824a4f38e683728cc6dd72367f6b8bad0a43428b1b3b987a3087adf413
    dest: /var/cache/homeassistant/hacs.zip
    mode: "0644"
    owner: root
    group: root
  become: true
  tags:
    - homeassistant

- name: Unpack HACS
  when: not (ansible_check_mode and homeassistant_user.changed)
  unarchive:
    src: /var/cache/homeassistant/hacs.zip
    remote_src: true
    dest: /mnt/services/homeassistant/custom_components/hacs
    creates: /mnt/services/homeassistant/custom_components/hacs/manifest.json
    owner: homeassistant
    group: homeassistant
  register: homeassistant_hacs_unpack
  become: true
  tags:
    - homeassistant

- name: Install homeassistant service
  import_role:
    name: systemd_unit
    tasks_from: install
  vars:
    systemd_unit_args:
      src: homeassistant.service.j2
      condition: "{{ not (ansible_check_mode and homeassistant_user.changed) }}"
  tags:
    - homeassistant

# Pre-stage chown'd layers so the unit's `systemctl start` doesn't blow
# through TimeoutSec=120 doing the per-file ID-mapped copy on first
# extraction. The +0:N:1 uidmap-override pattern doesn't compose with
# idmapped mounts, so podman falls back to a copy regardless of kernel
# support; we just move the cost out of the deadline. The staging
# container is removed; cached chown'd layers stay in storage and are
# reused by the unit's later `podman run`. Image name is read back
# from the just-installed unit so it stays in sync with the template.
- name: Detect homeassistant unit image
  when: not ansible_check_mode
  extract_podman_image:
    name: homeassistant
  register: homeassistant_unit_image
  become: true
  tags:
    - homeassistant

- name: Pre-pull homeassistant image
  when: not ansible_check_mode
  containers.podman.podman_image:
    name: "{{ homeassistant_unit_image.images[0] }}"
  register: homeassistant_image_pull
  become: true
  tags:
    - homeassistant

- name: Pre-stage homeassistant chown'd layers
  when:
    - not ansible_check_mode
    - homeassistant_image_pull.changed
  shell: |
    set -euo pipefail
    podman create --replace --name homeassistant-prestage \
      --uidmap=0:0:65536 \
      --uidmap=+0:{{ homeassistant_user.uid }}:1 \
      --gidmap=0:0:65536 \
      --gidmap=+0:{{ homeassistant_user.group }}:1 \
      {{ homeassistant_unit_image.images[0] }} true
    podman rm homeassistant-prestage
  args:
    executable: /bin/bash
  become: true
  tags:
    - homeassistant

- name: Enable homeassistant service
  import_role:
    name: systemd_unit
    tasks_from: service
  vars:
    systemd_unit_args:
      src: homeassistant
      condition: "{{ not (ansible_check_mode and (homeassistant_service_result.changed or homeassistant_user.changed)) }}"
      restart: "{{ homeassistant_service_result.changed or homeassistant_image_pull.changed or homeassistant_conf.changed or homeassistant_hacs_unpack.changed or homeassistant_config_files.changed }}"
  tags:
    - homeassistant

# HA's recorder writes very heavily (every state change, every poll);
# ZHA's zigpy DB sees fewer writes but the same fsync amplification.
# WAL halves the per-commit fsync count and lets readers not block the
# writer. The mode is persisted in the DB header, so this is one-shot
# (probe returns 'wal' on subsequent applies). DBs are skipped if
# absent (first apply before HA has created them); the second apply
# after HA's first cold start lands the conversion.
- name: Read sqlite journal mode
  command: sqlite3 -readonly /mnt/services/sqlite/homeassistant/{{ item }} 'PRAGMA journal_mode;'
  register: homeassistant_sqlite_pragma
  changed_when: false
  failed_when: false
  become: true
  loop:
    - home-assistant_v2.db
    - zigbee.db
  loop_control:
    label: "{{ item }}"
  tags:
    - homeassistant

- name: Convert sqlite to WAL
  command: sqlite3 /mnt/services/sqlite/homeassistant/{{ item.item }} 'PRAGMA journal_mode=WAL;'
  when:
    - item.rc == 0
    - (item.stdout | default('') | trim) not in ['wal', '']
  changed_when: true
  become: true
  loop: "{{ homeassistant_sqlite_pragma.results }}"
  loop_control:
    label: "{{ item.item }}"
  tags:
    - homeassistant

- name: Configure nginx
  import_role:
    name: nginx
    tasks_from: site
  vars:
    nginx_site_args:
      subdomain: homeassistant
      proxy_pass: http://localhost:{{ service_ports.homeassistant }}/
      csp_additional: "connect-src tile.openstreetmap.org a.tile.openstreetmap.org b.tile.openstreetmap.org c.tile.openstreetmap.org {{ nginx_csp_default }}; img-src *.cartocdn.com *.home-assistant.io {{ nginx_csp_default }}"
      location_conf: |
        add_header Access-Control-Allow-Origin "https://brands.home-assistant.io";
  tags:
    - homeassistant

- name: Install vscode editor
  import_role:
    name: vscode
    tasks_from: module
  vars:
    vscode_user:
      uid: "{{ homeassistant_user.uid }}"
      group: "{{ homeassistant_user.group }}"
      changed: "{{ homeassistant_user.changed }}"
    vscode_name: vscode_ha
    vscode_path: vscode_ha
    vscode_subdomain: vscode_ha
    vscode_mount: /mnt/services/homeassistant
    vscode_workspace: homeassistant
    vscode_port: 3002
    vscode_csp_frame_ancestors: https://homeassistant.{{ inventory_hostname }}.{{ domain }}/ 'self'
  tags:
    - homeassistant
