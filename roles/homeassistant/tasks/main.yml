- name: Create internal podman network
  # 10.123.4.96/30 — usable .97/.98. The earlier choice of .100/30
  # (.101/.102) collided with lab's host IP 10.123.4.102 on eth0.4
  # (host_vars/lab.yml: vlans.iot.addresses), and netavark
  # auto-assigned HA the colliding .102. Two MACs claiming the same
  # IP made every HA-originated frame on the IoT VLAN log as a martian
  # on the host. Keep this block clear of any address that lab itself
  # carries on the .4 VLAN.
  containers.podman.podman_network:
    name: iot_macvlan
    subnet: 10.123.4.96/30
    driver: macvlan
    opt:
      parent: iot
  become: true
  tags:
    - homeassistant

# Join the mosquitto-owned network so home-assistant can resolve `mosquitto`
# via aardvark-dns. See paperless/tasks/main.yml for the rationale.
- name: Create mosquitto podman network
  when: not ansible_check_mode
  containers.podman.podman_network:
    name: mosquitto
    disable_dns: false
    state: present
  become: true
  tags:
    - homeassistant

- name: Set up homeassistant system user, group, and config dir
  import_role:
    name: service_user
    tasks_from: user
  vars:
    service_user_args:
      name: homeassistant
  tags:
    - homeassistant

- name: Create sqlite directory
  when: not (ansible_check_mode and homeassistant_user.changed)
  file:
    dest: /mnt/services/sqlite/homeassistant
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

# ZHA stores its zigbee.db under /config alongside YAML/automations. The
# recorder DB already lives on the sqlite dataset via /config/db; pull
# zigbee.db onto the same dataset by symlinking through /config/db so
# SQLite resolves the symlink before computing WAL placement. The symlink
# target is dangling on the host (no /config/db there) but resolves
# inside the container — force: true bypasses ansible's host-side check.
- name: Symlink ZHA zigbee.db onto sqlite dataset
  file:
    src: /config/db/zigbee.db
    dest: /mnt/services/homeassistant/zigbee.db
    state: link
    owner: homeassistant
    group: homeassistant
    follow: false
    force: true
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant
  template:
    src: configuration.yaml.j2
    dest: /mnt/services/homeassistant/configuration.yaml
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    backup: true
    block_start_string: "<<%"
    block_end_string: "%>>"
    variable_start_string: "<<<"
    variable_end_string: ">>>"
  register: homeassistant_conf
  become: true
  tags:
    - homeassistant

- name: Create custom_templates directory
  file:
    dest: /mnt/services/homeassistant/custom_templates
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant macros
  copy:
    src: macros.jinja
    dest: /mnt/services/homeassistant/custom_templates/macros.jinja
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    backup: true
  register: homeassistant_macros
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant sensors
  copy:
    src: sensors.yaml
    dest: /mnt/services/homeassistant/sensors.yaml
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    backup: true
  register: homeassistant_sensors
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant templates
  copy:
    src: templates.yaml
    dest: /mnt/services/homeassistant/templates.yaml
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    backup: true
  register: homeassistant_templates
  become: true
  tags:
    - homeassistant

- name: Ensure files exist
  file:
    path: /mnt/services/homeassistant/{{ item }}
    owner: homeassistant
    group: homeassistant
    mode: "0600"
    state: touch
    modification_time: preserve
    access_time: preserve
  register: homeassistant_files
  become: true
  with_items:
    - automations.yaml
    - covers.yaml
    - scripts.yaml
    - scenes.yaml
  tags:
    - homeassistant

- name: Create blueprints directory
  file:
    dest: /mnt/services/homeassistant/blueprints
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Create automation directory
  file:
    dest: /mnt/services/homeassistant/blueprints/automation
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Configure homeassistant blueprints
  copy:
    src: blueprints/automation/hvac_dry_mode.yaml
    dest: /mnt/services/homeassistant/blueprints/automation/hvac_dry_mode.yaml
    owner: homeassistant
    group: homeassistant
    mode: "0644"
    backup: true
  register: homeassistant_blueprints
  become: true
  tags:
    - homeassistant

- name: Create www directory
  file:
    dest: /mnt/services/homeassistant/www
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  register: homeassistant_www_dir
  tags:
    - homeassistant

- name: Get stats of the thermal_comfort_icons.js file
  stat:
    path: /mnt/services/homeassistant/www/thermal_comfort_icons.js
  register: thermal_comfort_icons_check
  become: true
  tags:
    - homeassistant

- name: Install thermal_comfort_icons if needed
  when: not thermal_comfort_icons_check.stat.exists or thermal_comfort_icons_check.stat.checksum != '7a37a9c4532308923adfc1378715cf09aaaa4d13'
  become: true
  tags:
    - homeassistant
  block:
    - name: Download thermal_comfort_icons
      check_mode: false
      get_url:
        url: "{{ mirror_github }}/rautesamtr/thermal_comfort_icons/archive/refs/tags/1.3.0.tar.gz"
        checksum: sha256:fc790c528f223c15eef0920bb66b0f1cb0986d41e8a447cf21ee4dae0750aa53
        dest: /tmp/thermal_comfort_icons.tar.gz
        mode: "0644"
        owner: root
        group: root

    - name: Install thermal_comfort_icons
      when: not (ansible_check_mode and homeassistant_www_dir.changed)
      unarchive:
        src: /tmp/thermal_comfort_icons.tar.gz
        remote_src: true
        dest: /mnt/services/homeassistant/www
        mode: "0644"
        owner: homeassistant
        group: homeassistant
        include: [ thermal_comfort_icons-1.3.0/dist/thermal_comfort_icons.js ]
        list_files: true
        extra_opts: [ --strip-components=2 ]
      register: thermal_comfort_icons_installed

    - name: Delete tar file
      file:
        path: /tmp/thermal_comfort_icons.tar.gz
        state: absent

- name: Create custom_components directory
  file:
    dest: /mnt/services/homeassistant/custom_components
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  tags:
    - homeassistant

- name: Install unzip
  apt:
    pkg:
      - unzip
    cache_valid_time: 3600
  become: true
  tags:
    - homeassistant

- name: Create thermal_comfort directory
  file:
    dest: /mnt/services/homeassistant/custom_components/thermal_comfort
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  register: homeassistant_thermal_comfort_dir
  tags:
    - homeassistant

- name: Get stats of the thermal_comfort custom_component
  stat:
    path: /mnt/services/homeassistant/custom_components/thermal_comfort/manifest.json
  register: thermal_comfort_check
  become: true
  tags:
    - homeassistant

- name: Install thermal_comfort if needed
  when: not thermal_comfort_check.stat.exists
  become: true
  tags:
    - homeassistant
  block:
    - name: Download thermal_comfort
      check_mode: false
      get_url:
        url: "{{ mirror_github }}/dolezsa/thermal_comfort/releases/download/2.2.5/thermal_comfort.zip"
        checksum: sha256:c4ea4903f8f3ce787ddc4d0aef34397a1872431fa8e046589dc341bb8cbbcec9
        dest: /tmp/thermal_comfort.zip
        mode: "0644"
        owner: root
        group: root

    - name: Install thermal_comfort
      when: not (ansible_check_mode and homeassistant_thermal_comfort_dir.changed)
      unarchive:
        src: /tmp/thermal_comfort.zip
        remote_src: true
        dest: /mnt/services/homeassistant/custom_components/thermal_comfort
        mode: "a+rX"
        owner: homeassistant
        group: homeassistant
        list_files: true
      register: thermal_comfort_installed

    - name: Delete tar file
      file:
        path: /tmp/thermal_comfort.zip
        state: absent

- name: Create hacs directory
  file:
    dest: /mnt/services/homeassistant/custom_components/hacs
    state: directory
    owner: homeassistant
    group: homeassistant
    mode: "0755"
  become: true
  register: homeassistant_hacs_dir
  tags:
    - homeassistant

- name: Get stats of the hacs custom_component
  stat:
    path: /mnt/services/homeassistant/custom_components/hacs/manifest.json
  register: hacs_check
  become: true
  tags:
    - homeassistant

- name: Install hacs if needed
  when: not hacs_check.stat.exists
  become: true
  tags:
    - homeassistant
  block:
    - name: Download hacs
      check_mode: false
      get_url:
        url: "{{ mirror_github }}/hacs/integration/releases/download/2.0.5/hacs.zip"
        checksum: sha256:97be6b824a4f38e683728cc6dd72367f6b8bad0a43428b1b3b987a3087adf413
        dest: /tmp/hacs.zip
        mode: "0644"
        owner: root
        group: root

    - name: Install hacs
      when: not (ansible_check_mode and homeassistant_hacs_dir.changed)
      unarchive:
        src: /tmp/hacs.zip
        remote_src: true
        dest: /mnt/services/homeassistant/custom_components/hacs
        mode: "a+rX"
        owner: homeassistant
        group: homeassistant
        list_files: true
      register: hacs_installed

    - name: Delete tar file
      file:
        path: /tmp/hacs.zip
        state: absent

- name: Install homeassistant service
  import_role:
    name: systemd_unit
    tasks_from: install
  vars:
    systemd_unit_args:
      src: homeassistant.service.j2
      condition: "{{ not (ansible_check_mode and homeassistant_user.changed) }}"
  tags:
    - homeassistant

# Pre-stage chown'd layers so the unit's `systemctl start` doesn't blow
# through TimeoutSec=120 doing the per-file ID-mapped copy on first
# extraction. The +0:N:1 uidmap-override pattern doesn't compose with
# idmapped mounts, so podman falls back to a copy regardless of kernel
# support; we just move the cost out of the deadline. The staging
# container is removed; cached chown'd layers stay in storage and are
# reused by the unit's later `podman run`. Image name is read back
# from the just-installed unit so it stays in sync with the template.
- name: Detect homeassistant unit image
  when: not ansible_check_mode
  extract_podman_image:
    name: homeassistant
  register: homeassistant_unit_image
  become: true
  tags:
    - homeassistant

- name: Pre-pull homeassistant image
  when: not ansible_check_mode
  containers.podman.podman_image:
    name: "{{ homeassistant_unit_image.images[0] }}"
  register: homeassistant_image_pull
  become: true
  tags:
    - homeassistant

- name: Pre-stage homeassistant chown'd layers
  when:
    - not ansible_check_mode
    - homeassistant_image_pull.changed
  shell: |
    set -euo pipefail
    podman create --replace --name homeassistant-prestage \
      --uidmap=0:0:65536 \
      --uidmap=+0:{{ homeassistant_user.uid }}:1 \
      --gidmap=0:0:65536 \
      --gidmap=+0:{{ homeassistant_user.group }}:1 \
      {{ homeassistant_unit_image.images[0] }} true
    podman rm homeassistant-prestage
  args:
    executable: /bin/bash
  become: true
  tags:
    - homeassistant

- name: Enable homeassistant service
  import_role:
    name: systemd_unit
    tasks_from: service
  vars:
    systemd_unit_args:
      src: homeassistant
      condition: "{{ not (ansible_check_mode and (systemd_unit.changed or homeassistant_user.changed)) }}"
      restart: "{{ systemd_unit.changed or homeassistant_conf.changed or thermal_comfort_icons_installed.changed or thermal_comfort_installed.changed or hacs_installed.changed or homeassistant_macros.changed or homeassistant_sensors.changed or homeassistant_templates.changed or homeassistant_blueprints.changed }}"
  tags:
    - homeassistant

- name: Configure nginx
  import_role:
    name: nginx
    tasks_from: site
  vars:
    nginx_site_args:
      subdomain: homeassistant
      proxy_pass: http://localhost:{{ service_ports.homeassistant }}/
      csp_additional: "connect-src *.openstreetmap.org {{ nginx_csp_default }}; img-src *.cartocdn.com *.home-assistant.io {{ nginx_csp_default }}"
      location_conf: |
        add_header Access-Control-Allow-Origin "https://brands.home-assistant.io";
  tags:
    - homeassistant

- name: Install vscode editor
  import_role:
    name: vscode
    tasks_from: module
  vars:
    vscode_user: "{{ homeassistant_user }}"
    vscode_name: vscode_ha
    vscode_path: vscode_ha
    vscode_subdomain: vscode_ha
    vscode_mount: /mnt/services/homeassistant
    vscode_workspace: homeassistant
    vscode_port: 3002
    vscode_csp_frame_ancestors: https://homeassistant.{{ inventory_hostname }}.{{ domain }}/ 'self'
  tags:
    - homeassistant
