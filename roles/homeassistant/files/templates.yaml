# Placeholder. Operator-private template list lives in the main worktree only;
# gitignore keeps real content untracked. This stub (empty list) exists so
# the role's `copy:` task converges on a fresh clone / in CI.
[]
