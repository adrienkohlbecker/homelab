# Zigbee2MQTT's console transport emits Winston JSON. Journald remains the
# event-time authority; normalize.lua removes the duplicate application stamp.
[PARSER]
    Name    z2m_json
    Format  json
