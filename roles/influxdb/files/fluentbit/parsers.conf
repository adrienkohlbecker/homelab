# InfluxDB emits logfmt on stdout when the destination is not a TTY. Journald's
# arrival timestamp remains authoritative, so normalize.lua removes the parsed
# application timestamp after promoting the other fields.
[PARSER]
    Name                 influxdb_logfmt
    Format               logfmt
    Logfmt_No_Bare_Keys  true
