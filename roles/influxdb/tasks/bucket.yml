# Provision a per-tenant (org, user, bucket, write-auth) into a
# running influxdb container. Idempotent via query-then-create
# pairs. Each task uses `influx ... --json` and parses via
# `from_json` to avoid the column-shape drift that tab-cut parsing
# carries across influxdb-cli versions.
#
# Inputs flow through `influxdb_bucket_args` (dict) per the
# helper-role pattern in CLAUDE.md so each call replaces the dict
# wholesale and inter-call vars-leak can't happen.
#
# Required keys:
#   org    -- influxdb org name (must already exist; not created here)
#   user   -- user name (created if missing, attached to <org>)
#   bucket -- bucket name (created under <org> if missing)
#
# Outputs: none at play scope. The earlier `influxdb_bucket_org_id` /
# `influxdb_bucket_id` set_facts were a cross-call leak waiting to fire
# (a second caller in the same play would overwrite the first's facts);
# the ids are now block-scoped vars, lazily resolved from the query
# registers each time they're referenced inside the block.
#
# Operator extracts the resulting write-auth token post-bootstrap via
#   podman exec influxdb influx auth list --user <user> --json
# and pastes it into the consumer (e.g. HA's UI integration).

- name: bucket | Assert args
  assert:
    that:
      - influxdb_bucket_args.org    is defined
      - influxdb_bucket_args.user   is defined
      - influxdb_bucket_args.bucket is defined
    fail_msg: >-
      influxdb_bucket requires influxdb_bucket_args.{org,user,bucket}.

# Block until the influxdb container is up and answering. `podman exec`
# would otherwise fail with a low-context "no such container" if the
# caller runs against a host where influxdb is still booting (e.g.
# right after a `--tags` run that includes both roles), or with a
# transient HTTP/timeout if influxd is still starting.
- name: bucket | Wait for influxdb to answer ping
  command: podman exec influxdb influx ping
  register: _influxdb_bucket_ready
  changed_when: false
  retries: 30
  delay: 2
  until: _influxdb_bucket_ready.rc == 0
  become: true

- name: bucket | Query org {{ influxdb_bucket_args.org }}
  command: podman exec influxdb influx org list --name {{ influxdb_bucket_args.org }} --json
  register: _influxdb_bucket_org_query
  changed_when: false
  check_mode: false
  failed_when: _influxdb_bucket_org_query.rc != 0
  become: true

# selectattr('name','equalto', ...) is defensive: influx-cli 2.x's
# `--name` is an exact filter today, but `length > 0` against the
# bare result would silently accept a future patch that turned the
# filter into a prefix/substring match. The org is bootstrapped only
# for `home` (DOCKER_INFLUXDB_INIT_ORG); any other org has to exist
# already, so the assert above the generic length-0 fail gives the
# operator a concrete next step.
- name: bucket | Assert org {{ influxdb_bucket_args.org }} exists
  assert:
    that:
      - (_influxdb_bucket_org_query.stdout
          | from_json
          | selectattr('name', 'equalto', influxdb_bucket_args.org)
          | list | length) > 0
    fail_msg: >-
      influxdb org '{{ influxdb_bucket_args.org }}' not found.
      The influxdb role's main.yml bootstraps only `home` (via
      DOCKER_INFLUXDB_INIT_ORG); any other org must be pre-created
      out-of-band with `podman exec influxdb influx org create
      --name {{ influxdb_bucket_args.org }}`.

# block-scoped lazy vars replace the earlier set_fact pattern. Each
# reference re-evaluates from the relevant query register; values
# never enter host-fact scope, so a subsequent import_role with
# different inputs can't pick up the previous call's ids.
- name: bucket | Provision tenant {{ influxdb_bucket_args.user }} / {{ influxdb_bucket_args.bucket }} in org {{ influxdb_bucket_args.org }}
  vars:
    _influxdb_org_id: >-
      {{ (_influxdb_bucket_org_query.stdout
          | from_json
          | selectattr('name', 'equalto', influxdb_bucket_args.org)
          | first).id }}
    _influxdb_bucket_id: >-
      {{ (_influxdb_bucket_bucket_id_query.stdout
          | from_json
          | selectattr('name', 'equalto', influxdb_bucket_args.bucket)
          | first).id }}
  block:
    - name: bucket | Query user {{ influxdb_bucket_args.user }}
      command: podman exec influxdb influx user list --name {{ influxdb_bucket_args.user }} --json
      register: _influxdb_bucket_user_query
      changed_when: false
      check_mode: false
      failed_when: false
      become: true

    - name: bucket | Create user {{ influxdb_bucket_args.user }}  # noqa: no-changed-when -- when: gates execution, like creates: did
      when: >-
        _influxdb_bucket_user_query.rc != 0
        or (_influxdb_bucket_user_query.stdout | from_json | length) == 0
      command: podman exec influxdb influx user create --org-id {{ _influxdb_org_id }} --name {{ influxdb_bucket_args.user }}
      become: true

    - name: bucket | Query bucket {{ influxdb_bucket_args.bucket }}
      command: podman exec influxdb influx bucket list --org-id {{ _influxdb_org_id }} --name {{ influxdb_bucket_args.bucket }} --json
      register: _influxdb_bucket_bucket_query
      changed_when: false
      check_mode: false
      failed_when: false
      become: true

    - name: bucket | Create bucket {{ influxdb_bucket_args.bucket }}  # noqa: no-changed-when -- when: gates execution, like creates: did
      when: >-
        _influxdb_bucket_bucket_query.rc != 0
        or (_influxdb_bucket_bucket_query.stdout | from_json | length) == 0
      command: podman exec influxdb influx bucket create --org-id {{ _influxdb_org_id }} --name {{ influxdb_bucket_args.bucket }}
      become: true

    # Re-query post-create so the auth task always sees a concrete bucket
    # id. The conditional create above doesn't emit a usable id (the
    # upstream `influx bucket create` output schema changed across 2.x
    # patch releases).
    - name: bucket | Query bucket id for {{ influxdb_bucket_args.bucket }}
      command: podman exec influxdb influx bucket list --org-id {{ _influxdb_org_id }} --name {{ influxdb_bucket_args.bucket }} --json
      register: _influxdb_bucket_bucket_id_query
      changed_when: false
      check_mode: false
      failed_when: >-
        _influxdb_bucket_bucket_id_query.rc != 0
        or (_influxdb_bucket_bucket_id_query.stdout
            | from_json
            | selectattr('name', 'equalto', influxdb_bucket_args.bucket)
            | list | length) == 0
      become: true

    - name: bucket | Query auth for {{ influxdb_bucket_args.user }}
      command: podman exec influxdb influx auth list --user {{ influxdb_bucket_args.user }} --json
      register: _influxdb_bucket_auth_query
      changed_when: false
      check_mode: false
      failed_when: false
      become: true

    # `influx auth list --json` emits permissions as bare strings shaped
    # `<action>:orgs/<orgid>/buckets/<bucketid>` (or `<action>:/buckets`,
    # `<action>:/orgs`, etc. for non-scoped perms), NOT as the dict-shaped
    # {action, resource: {type, id}} structure the InfluxDB HTTP API
    # returns. selectattr('action', ...) crashes on strings. Regex-match
    # the bucket-scoped write permission directly, anchored on both the
    # org id and the bucket id -- earlier shape `^write:.*buckets/<id>$`
    # would false-positive on a cross-org perm pointing at the same
    # bucket id (theoretically possible, easy to forestall).
    - name: bucket | Create write-auth for bucket {{ influxdb_bucket_args.bucket }}  # noqa: no-changed-when -- when: gates execution, like creates: did
      when: >-
        (_influxdb_bucket_auth_query.stdout | default('[]') | from_json)
        | selectattr('permissions', 'defined')
        | map(attribute='permissions') | flatten
        | select('search', '^write:orgs/' ~ _influxdb_org_id ~ '/buckets/' ~ _influxdb_bucket_id ~ '$')
        | list | length == 0
      command: >-
        podman exec influxdb influx auth create
        --org-id {{ _influxdb_org_id }}
        --user {{ influxdb_bucket_args.user }}
        --write-bucket {{ _influxdb_bucket_id }}
      become: true
