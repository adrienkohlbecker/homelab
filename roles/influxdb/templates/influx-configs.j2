# Managed default for the in-container CLI; influxd does not read this file.
[default]
  url = "http://localhost:8086"
  token = "{{ influxdb_root_token }}"
  org = "{{ influxdb_root_org }}"
  active = true
