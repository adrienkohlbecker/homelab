# Together the negative and authenticated probes prove the configured API key
# is required and active.
- name: _verify | Bazarr /api/system/status rejects unauth'd requests
  uri:
    url: http://localhost:{{ service_ports.bazarr }}/api/system/status
    status_code: 401

# Header form keeps the key out of journald.
- name: _verify | Bazarr answers /api/system/status with our apikey
  uri:
    url: http://localhost:{{ service_ports.bazarr }}/api/system/status
    headers:
      X-API-KEY: "{{ bazarr_api_key }}"
    status_code: 200
  register: _bazarr_http
  failed_when: (_bazarr_http.json.data | default({})) | length == 0

- name: _verify | Wait for normalized bazarr record in lnav store
  command:
    argv:
      - python3
      - -c
      - |
        import json
        import sys

        with open(sys.argv[1], encoding="utf-8") as handle:
            for line in handle:
                try:
                    record = json.loads(line)
                except json.JSONDecodeError:
                    continue
                fields = record.get("fields", {})
                if record.get("service") == "bazarr" and fields.get("source_line") is not None:
                    sys.exit(0)
        sys.exit(1)
      - /var/log/fluent-bit/lnav.jsonl
  register: _bazarr_lnav_record
  changed_when: false
  retries: 30
  delay: 2
  until: _bazarr_lnav_record.rc == 0
  become: true

# The loopback API probes bypass nginx; verify browser traffic stays gated.
- name: _verify | Verify Bazarr Authelia gate
  import_role:
    name: nginx
    tasks_from: verify_authelia_gate
  vars:
    nginx_verify_authelia_gate_args:
      subdomain: bazarr

- name: _verify | Exercise bazarr fluent-bit record contracts
  import_role:
    name: fluentbit
    tasks_from: verify_extension
  vars:
    fluentbit_verify_args:
      name: bazarr
      records:
        - MESSAGE: "2099-01-02 03:04:05,678 - root (abcdef) : CRITICAL (server:42) - bazarr-valid"
          CONTAINER_TAG: bazarr
          SYSLOG_IDENTIFIER: conmon
          SYSTEMD_UNIT: bazarr.service
          parser_status: forged
        - MESSAGE: bazarr-raw
          CONTAINER_TAG: bazarr
          SYSLOG_IDENTIFIER: conmon
          SYSTEMD_UNIT: bazarr.service
      expected:
        - message: bazarr-valid
          level: fatal
          service: bazarr
          fields.source_line: 42
        - message: bazarr-raw
          service: bazarr
- name: _verify | Exercise alternate service storage layouts
  include_tasks: "{{ ansible_config_file | dirname }}/test/playbooks/tasks/_verify_service_storage.yml"
  vars:
    _storage_service: bazarr
    _storage_group: media
    _storage_mounts: [ services, media ]

- name: _verify | Render the production config for a physical host
  template:
    src: config.yaml.j2
    dest: /tmp/bazarr-physical.yaml
    mode: "0600"
  vars:
    qemu_test: false
  diff: false
  become: true

- name: _verify | Verify physical-host integration settings
  command:
    argv:
      - python3
      - -c
      - |
        import pathlib
        import sys

        config = pathlib.Path(sys.argv[1]).read_text()
        assert "use_radarr: true" in config
        assert "use_sonarr: true" in config
      - /tmp/bazarr-physical.yaml
  changed_when: false
  become: true

- name: _verify | Remove the physical-host config
  file:
    path: /tmp/bazarr-physical.yaml
    state: absent
  become: true
