# apt_source is a helper role accessed only via `tasks_from: configure`.
# main.yml exists so testrole.py can target this role; the actual logic lives
# in configure.yml and the standalone test scenario in _verify.yml.
