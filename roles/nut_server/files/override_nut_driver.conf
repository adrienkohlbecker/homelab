[Unit]
# The Eaton is polled through snmp-ups over the management VLAN. Start the
# packaged driver only after network-online so upsdrvctl does not burn retries
# before the route to the UPS exists.
Wants=network-online.target
After=network-online.target
