# nut_server runs on every home host (site.yml, all:!bunk:!fox), not just
# lab: each host independently SNMP-polls the one Eaton UPS and exposes its
# own upsd, monitored by its own upsmon (roles/nut_monitor, ordered right
# after this role) — so no host depends on a peer NUT server being up, and
# the per-host ups.conf low-battery overrides form the staged shutdown
# ladder (see defaults/main.yml and host_vars/pug.yml). upsd also serves
# Home Assistant (on lab) + operator `upsc` reads. The nut-monitor unit
# itself (enablement, upsmon.conf) is entirely nut_monitor's; this role
# does not touch it.

- name: Assert required nut secrets are set
  assert:
    that:
      - nut_upsmon_password is defined and (nut_upsmon_password | length > 0)
      - nut_homeassistant_password is defined and (nut_homeassistant_password | length > 0)
      - nut_winnut_password is defined and (nut_winnut_password | length > 0)
    fail_msg: >-
      nut_upsmon_password, nut_homeassistant_password and nut_winnut_password must be set
      (group_vars/{prod,test}.yml); upsd.users renders them verbatim.
  tags:
    - nut_server

- name: Install nut
  apt:
    pkg:
      - nut-snmp
      - nut-cgi
      - fcgiwrap
    cache_valid_time: 3600
  become: true
  register: nut_server_apt
  tags:
    - nut_server

- name: Configure nut nut.conf  # noqa: require-validate
  when: not (ansible_check_mode and nut_server_apt.changed)
  copy:
    src: nut.conf
    dest: /etc/nut/nut.conf
    backup: true
    owner: root
    group: nut
    mode: "0640"
  become: true
  register: nut_server_nut_conf
  tags:
    - nut_server

- name: Configure nut ups.conf
  when: not (ansible_check_mode and nut_server_apt.changed)
  template:
    src: ups.conf.j2
    dest: /etc/nut/ups.conf
    owner: root
    group: nut
    mode: "0640"
    backup: true
    # Prepend a dummy section so Python 3.12 accepts the global `maxretry`
    # directive that precedes the first named UPS section.
    validate: python3 -c 'import configparser,sys; configparser.ConfigParser(allow_no_value=True).read_string("[__global__]\n" + open(sys.argv[1]).read())' "%s"
  register: nut_server_ups_conf
  become: true
  tags:
    - nut_server

- name: Configure nut upsd.conf  # noqa: require-validate
  when: not (ansible_check_mode and nut_server_apt.changed)
  template:
    src: upsd.conf.j2
    dest: /etc/nut/upsd.conf
    backup: true
    owner: root
    group: nut
    mode: "0640"
  become: true
  register: nut_server_upsd_conf
  tags:
    - nut_server

- name: Configure nut upsd.users
  when: not (ansible_check_mode and nut_server_apt.changed)
  template:
    src: upsd.users.j2
    dest: /etc/nut/upsd.users
    owner: root
    group: nut
    mode: "0640"
    backup: true
    validate: python3 -c 'import configparser; configparser.ConfigParser(allow_no_value=True).read("%s")'
  become: true
  register: nut_server_upsd_users
  tags:
    - nut_server

- name: Configure nut hosts.conf  # noqa: require-validate
  when: not (ansible_check_mode and nut_server_apt.changed)
  copy:
    src: hosts.conf
    dest: /etc/nut/hosts.conf
    backup: true
    owner: root
    group: root
    mode: "0644"
  become: true
  tags:
    - nut_server

- name: Configure nut upsset.conf  # noqa: require-validate
  when: not (ansible_check_mode and nut_server_apt.changed)
  copy:
    src: upsset.conf
    dest: /etc/nut/upsset.conf
    backup: true
    owner: root
    group: root
    mode: "0644"
  become: true
  tags:
    - nut_server

# nut-driver is static (WantedBy=nut-server.service), so it can't be
# `disabled` -- nut-server drags it in on boot regardless. Under qemu_test
# there is no UPS for snmp-ups to reach (external_ips.eaton answers nothing),
# so the driver only times out and fails; mask it so nut-server's Wants is
# skipped instead of starting a doomed driver. Prod is left untouched: this
# task is qemu-only, so the ordering drop-in and enable below own the driver
# on prod, and a manual operator mask is respected rather than silently
# unmasked -- systemd_unit's "refuse to overwrite masked unit" guard then
# fails the converge instead, surfacing the mask.
- name: Mask nut-driver when there is no UPS to talk to
  when: qemu_test | bool and not (ansible_check_mode and nut_server_apt.changed)
  systemd:
    name: nut-driver@eaton.service
    masked: true
  become: true
  tags:
    - nut_server

- name: Order nut-driver after network-online
  when: not qemu_test
  import_role:
    name: systemd_unit
    tasks_from: install
  vars:
    systemd_unit_args:
      for: nut-driver@eaton.service
      src: override_nut_driver.conf
      condition: "{{ not (ansible_check_mode and nut_server_apt.changed) }}"
  tags:
    - nut_server

- name: Enable nut-driver service
  when: not qemu_test
  import_role:
    name: systemd_unit
    tasks_from: service
  vars:
    systemd_unit_args:
      src: nut-driver@eaton.service
      condition: "{{ not (ansible_check_mode and nut_server_apt.changed) }}"
      restart: "{{ nut_server_nut_conf.changed or nut_server_ups_conf.changed }}"
  tags:
    - nut_server

- name: Enable nut-server service
  import_role:
    name: systemd_unit
    tasks_from: service
  vars:
    systemd_unit_args:
      src: nut-server.service
      condition: "{{ not (ansible_check_mode and nut_server_apt.changed) }}"
      restart: "{{ nut_server_nut_conf.changed or nut_server_upsd_conf.changed or nut_server_upsd_users.changed }}"
      start: "{{ not qemu_test }}" # TODO: no hardware UPS to talk to
  tags:
    - nut_server

- name: Configure nginx
  import_role:
    name: nginx
    tasks_from: site
  vars:
    nginx_site_args:
      subdomain: nut
      auth: required
      server_conf: |
        location = / {
          return 301 /upsstats.cgi;
        }
      location_conf: |
        gzip off;
        root /usr/lib/cgi-bin/nut;

        # Hand only *.cgi requests to fcgiwrap, which exec's SCRIPT_FILENAME
        # directly -- an unscoped pass would run whatever file an arbitrary
        # path resolves to under the cgi-bin root. auth_request is inherited
        # from the enclosing `location /`, so this stays Authelia-gated.
        location ~ \.cgi$ {
          include /etc/nginx/fastcgi_params;

          fastcgi_pass  unix:/var/run/fcgiwrap.socket;
          fastcgi_param SCRIPT_FILENAME  /usr/lib/cgi-bin/nut$fastcgi_script_name;
        }
  tags:
    - nut_server
