# _setup.yml imports _test/nginx and _test/authelia_stub; nginx is
# pre-baked into box_deps (see packer/seed_deps.yml), so route there
# to skip the apt+nginx install on every test run.
machine: box_deps
