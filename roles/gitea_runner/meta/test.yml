# _setup.yml imports podman/install (after migrating off raw apt) and
# test/nginx (via the gitea role). box_deps pre-bakes both, saving
# ~140s of backports + podman + nginx install per test run.
machines:
  box_deps:
