# vim:ft=properties
LISTEN_ADDRESS=127.0.0.1:{{ service_ports.custom_exporter }}
NETDATA_CONTEXTS={% for name, metric in netdata_contexts.items() %}{{ name }}:{{ metric }}{% if not loop.last %},{% endif %}{% endfor +%}
NETDATA_COLLECTORS={% for name in netdata_collectors %}{{ name }}{% if not loop.last %},{% endif %}{% endfor +%}
