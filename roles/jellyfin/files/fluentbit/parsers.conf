# Jellyfin's Console sink emits rendered Compact Log Event Format JSON. Keep
# journald's arrival timestamp as the aggregate event time; the application
# timestamp is redundant and is removed by normalize.lua after parsing.
[PARSER]
    Name    jellyfin_clef
    Format  json
