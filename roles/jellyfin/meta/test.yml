# _setup.yml imports podman/install + nginx/test_fixture; both are pre-baked
# into box_deps (see packer/seed_deps.yml), so route there to skip
# podman + nginx install on every test run.
machines:
  box_deps:
