- name: monitor | Monitor cron entry
  file:
    path: /var/log/jobs/{{ item }}
    state: touch
    access_time: preserve
    modification_time: preserve
    owner: root
    group: root
    mode: "0644"
  loop: "{{ [cron_entry] | flatten(levels=1) }}"
  become: true
