# Headscale's JSON log format is zerolog's native object shape. Keep journald's
# arrival timestamp as the aggregate event time; normalize.lua removes the
# duplicate application timestamp after parsing.
[PARSER]
    Name    headscale_json
    Format  json
