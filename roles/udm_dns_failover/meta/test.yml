# The role's real target is the UDM (hosts: udm), a UniFi OS appliance with
# no qemu fixture. box exercises the portable surface — pre-write config
# validation, the /data on-boot recovery script, the VIP claim, and the
# heartbeat timer — on a stock Ubuntu VM. Firmware-wipe persistence itself
# can't be reproduced under qemu; _verify restarts on-boot.service to prove
# the complete recovery path is sound.
machines:
  box:
