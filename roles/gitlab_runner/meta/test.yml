# box_deps exercises Docker/AWS host artifacts; lab exercises cleanup when
# switching planted Docker state to the shell executor.
machines:
  box_deps:
  # Shell-executor fallback selected by host_vars/lab.yml.
  lab:
