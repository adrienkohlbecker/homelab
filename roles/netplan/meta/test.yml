# Resolute exercises Netplan 1.2 transaction rollback and DNS recovery after
# SSH returns.
machines:
  lab:
  minimal:
ubuntu:
  - resolute
skip:
  "minimal:resolute": >-
    Lab covers Resolute rollback and DNS recovery; Minimal covers origin removal
    on Noble.
