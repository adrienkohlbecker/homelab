# Force the opposite starting state so converge must recreate the thermald
# policy group.
- name: _setup | Remove power group (converge recreates it)
  group:
    name: power
    state: absent
  become: true
