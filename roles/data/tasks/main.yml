- name: Configure data datasets
  include_tasks:
    file: _dataset.yml
    apply:
      tags:
        - data
  loop: "{{ data_datasets }}"
  loop_control:
    label: "tank/{{ item.name }}"
  tags:
    - data
