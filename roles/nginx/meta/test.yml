# Release-divergent: nginx config depends on the distribution
machines:
  box:
  # Lab enables the inventory-driven static reverse proxies.
  lab:
ubuntu:
  - resolute
skip:
  "lab:resolute": >-
    Box Resolute covers the release-divergent nginx configuration; Lab Noble
    covers the inventory-driven static proxy paths.
