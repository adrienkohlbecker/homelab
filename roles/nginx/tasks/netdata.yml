- name: Configure web log metrics
  import_role:
    name: netdata
    tasks_from: config
  vars:
    netdata_args:
      src: web_log.conf
      dest: go.d/web_log.conf
      restart: "{{ nginx_netdata_restart | default(true) }}"
  tags:
    - nginx
