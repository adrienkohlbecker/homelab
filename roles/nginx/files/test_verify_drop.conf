# nginx _verify fixture: exercises the conf_d `src:` (file) input path. _verify
# drops it into conf.d/ via the nginx_conf_d helper, asserts the content landed,
# then removes it. A comment-only file is a valid (no-op) http{}-scope include.
# Content marker the test greps for: __nginx_verify_src_marker__
