- name: Configure fail2ban metrics
  import_role:
    name: netdata
    tasks_from: copy
  vars:
    netdata_args:
      src: fail2ban.conf
      dest: go.d/fail2ban.conf
      restart: "{{ netdata_restart }}"
  tags:
    - fail2ban
