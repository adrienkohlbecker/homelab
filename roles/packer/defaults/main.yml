# Noble's archive has enough kernel history to exercise Packer's aarch64
# kernel-update path. Resolute remains waived while its development archive
# carries only the image's running ABI; adding a release requires an explicit
# expectation instead of silently skipping the test.
packer_kernel_swap_expected_by_release:
  noble: true
  resolute: false
