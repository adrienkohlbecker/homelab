[Service]
# Ephemeral Podman interfaces can disappear between a dispatcher event and its
# networkctl lookup; retain all other dispatcher failures.
LogFilterPatterns=~^Failed to get interface \x22(veth|podman)
LogFilterPatterns=~^Unknown index .* seen, reloading interface list
