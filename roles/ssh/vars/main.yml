# Keep aligned with the sole HostKey directive in templates/00-hardening.conf.j2.
_ssh_host_private_key_file: ssh_host_ed25519_key

_ssh_host_key_files:
  - "{{ _ssh_host_private_key_file }}"
  - "{{ _ssh_host_private_key_file }}.pub"
