# Supported HostKey algorithms by order of preference. ecdsa is omitted:
# ed25519 + rsa cover every client here, and dropping it removes the weakest
# host-key type a downgrade-capable client could negotiate.
HostKey /etc/ssh/ssh_host_ed25519_key
HostKey /etc/ssh/ssh_host_rsa_key
# Advertise only the ed25519 server host key to connecting clients. The RSA key
# stays on disk as a fallback, but every client in this fleet supports ed25519.
HostKeyAlgorithms ssh-ed25519

# curve25519 only. The NIST P-curve kex (ecdh-sha2-nistp*) and DH group
# exchange (diffie-hellman-group-exchange-sha256) are dropped: every client
# here speaks curve25519, GEX security hinges on an unfiltered moduli file,
# and the nistp curves carry the well-known seed-provenance concern.
KexAlgorithms curve25519-sha256,curve25519-sha256@libssh.org
# AEAD ciphers only. The aes*-ctr fallbacks are dropped since every client
# supports chacha20-poly1305 or AES-GCM.
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com
MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com,umac-128-etm@openssh.com

# Password based logins are disabled - only public key based logins are allowed.
# PubkeyAuthentication is enabled explicitly: AuthenticationMethods controls
# which methods are *required*, not which are offered, so the publickey
# mandate below relies on this being on (it is the upstream default).
PubkeyAuthentication yes
# Restrict accepted client public-key types to ed25519 and RSA-SHA2 variants.
# Drops ecdsa-sha2-nistp* user keys — consistent with the kex policy that
# excludes NIST P-curves. Every key in authorized_keys here is ed25519.
PubkeyAcceptedAlgorithms ssh-ed25519,sk-ssh-ed25519@openssh.com,rsa-sha2-256,rsa-sha2-512
AuthenticationMethods publickey

# LogLevel VERBOSE logs user's key fingerprint on login. Needed to have a clear audit track of which key was using to log in.
LogLevel VERBOSE

# Log sftp level file access (read/write/etc.) that would not be easily logged otherwise.
Subsystem sftp  internal-sftp -f AUTHPRIV -l INFO

# Root login is not allowed for auditing reasons. This is because it's difficult to track which process belongs to which root user:
#
# On Linux, user sessions are tracking using a kernel-side session id, however, this session id is not recorded by OpenSSH.
# Additionally, only tools such as systemd and auditd record the process session id.
# On other OSes, the user session id is not necessarily recorded at all kernel-side.
# Using regular users in combination with /bin/su or /usr/bin/sudo ensure a clear audit track.
PermitRootLogin no

# To disable tunneled clear text passwords, change to no here!
PasswordAuthentication no
KbdInteractiveAuthentication no
# X11 forwarding opens a server-side display socket any local process can connect
# to; no use case for this operator.
X11Forwarding no

# Bound the unauthenticated login window. With pubkey-only auth there is no
# password to type, so 30s is ample and shrinks the pre-auth connection-slot
# window an attacker can hold open.
LoginGraceTime 30

# Reap dead sessions: probe every 5 min over the encrypted channel and
# disconnect after 3 missed replies (~15 min). The client transport answers
# these regardless of app activity, so active X11/TCP/agent/gpg forwarding
# stays up; only a vanished client (slept laptop, dropped link) is reaped,
# freeing its connection slot and any forwarded sockets.
ClientAliveInterval 300
ClientAliveCountMax 3

# Remove sockets before connecting (used for RemoteForward in gpg forwarding)
StreamLocalBindUnlink yes
