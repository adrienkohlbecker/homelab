/.worktrees
# Private in-place clones, owned by their sync workflows rather than this repo.
/notes
/roles/homeassistant/files/ha_gui_config
__pycache__
.vscode
test/out.tsv
test/out.tsv.prev
test/out/
test/out_*.log
# Checksum-verified aarch64 firmware fetched by `mise run test:firmware`.
test/firmware
/.ansible-mitogen-strategy
/packer/artifacts
/zbm-build
.DS_Store

# Track shared .claude state; ignore only per-user and scheduler state.
.claude/settings.local.json
.claude/scheduled_tasks.lock

mise.local.toml
