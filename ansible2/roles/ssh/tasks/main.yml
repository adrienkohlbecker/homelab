- name: Configure sshd
  copy:
    src: sshd_config
    dest: /etc/ssh/sshd_config.d/config
    owner: root
    group: root
    mode: "0644"
    backup: yes
  register: sshd_config
  become: yes
  tags:
    - ssh

- name: Restart ssh
  when: sshd_config.changed
  systemd:
    name: ssh
    state: restarted
  become: yes
  tags:
    - ssh
